import 'package:flutter/material.dart';

class Discovertrips extends StatelessWidget {
  const Discovertrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Discover Trips")));
  }
}
